// Export centrale per import puliti

// Database types
export * from './database';

// Auth types
export * from './auth';

// API types
export * from './api';

// Shared types (per frontend)
export * from './shared';

// Ora puoi importare così:
// import { User, Contract, CreateContractBody, SuccessResponse } from '@/types';